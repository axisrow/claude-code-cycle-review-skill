# Test PR — review target

This is a **dummy PR** used as a target for running `/cycle-review local` against,
to exercise the `/review`-via-`Skill`-tool local-review path end to end.

It intentionally contains a small, reviewable change so `/review` has something
to report on. Treat any findings as a test of the parsing/triage pipeline, not
as production work.

Closes #9.
